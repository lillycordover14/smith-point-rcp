/**
 * Smith Point RCP — Backend Configuration
 *
 * Set RCP_API_BASE to your deployed backend URL.
 *
 * Examples:
 *   "https://rcp-api.fly.dev"         ← Fly.io
 *   "https://rcp.up.railway.app"      ← Railway
 *   "http://localhost:8000"           ← local dev (default)
 *
 * Leave as empty string "" to run in demo mode (no backend needed).
 */
window.RCP_API_BASE = "";
